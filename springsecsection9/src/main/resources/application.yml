spring:
  config:
    import: classpath:database.yml
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org:
      springframework:
        security:
          web:
            org.springframework.security.web.FilterChainProxy: debug
